{
  "name": "autotuesday-kb-mcp",
  "version": "0.1.0",
  "description": "MCP server: gateway between each partner's Claude Code and the AutoTuesday org knowledge base. Carries the partner's identity (JWT) to Supabase so RLS enforces who-sees-what.",
  "type": "module",
  "bin": {
    "autotuesday-kb-mcp": "dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@supabase/supabase-js": "^2.45.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  }
}
